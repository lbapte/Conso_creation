version: 1
applications:
  - appRoot: "."  # The root of your repository is the app root in this case
    frontend:
      phases:
        preBuild:
          commands:
            - npm install
        build:
          commands:
            - cd build
            - npm run build
      artifacts:
        baseDirectory: build
        files:
          - '**/*'
      cache:
        paths:
          - node_modules/**/*