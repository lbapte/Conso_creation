version: 1
frontend:
  phases:
    preBuild:
      commands:
        - nvm install 10
        - nvm use 10
    build:
      commands:
        - npm install
        - npm run build
  artifacts:
    baseDirectory: build
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
